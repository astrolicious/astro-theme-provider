---
import config from "theme-playground/config";

interface Props {
	title?: string;
}

const { title } = Astro.props;
---

<h1>
  { Astro.slots.has('default')
    && <slot />
    || title
    || config.title
  }
</h1>