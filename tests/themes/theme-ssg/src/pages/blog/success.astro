---
import { Heading } from "theme-ssg:components";
import { Layout } from "theme-ssg:layouts";
import { getEntry } from "astro:content";

const entry = await getEntry('blog', 'success')
---

<Layout>
  <Heading>
    { entry.data.title }
  </Heading>
</Layout>
