---
import { Heading } from "theme-ssg:components";
import { Layout } from "theme-ssg:layouts";
import { getEntry } from "astro:content";

const entry = await getEntry('blog', 'fail')
---

<Layout>
  <Heading>
    { entry.data.title }
  </Heading>
</Layout>
