---
import config from "theme-ssg/config";

interface Props {
	title?: string;
}

const { title } = Astro.props;
---

<h1>
  { Astro.slots.has('default')
    && <slot />
    || title
    || config.title
  }
</h1>